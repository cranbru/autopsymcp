"""
Autopsy AI MCP Server  —  Improved Edition v2.1
=================================================
Integrates Sleuth Kit Autopsy digital forensics cases with Claude Desktop
by reading Autopsy's native SQLite case database (.db) directly.

No plugins. No REST API. No Anthropic API key. No modifications to Autopsy.

How it works:
  - Every Autopsy 4.x case produces a SQLite file at <CaseDir>/<CaseName>.db
  - Default case location: C:\\Users\\<YourUser>\\Documents\\<CaseName>\\
  - This server opens that file read-only and exposes its contents as MCP tools
  - Claude Desktop calls those tools, receives the forensic data, and reasons
    over it natively — Claude IS the analysis layer, no second API call needed

Find your .db path: Autopsy -> Case -> Case Properties

NEW in v2.1:
  - Auto-resolve case path: provide just a case name and the server finds the .db
  - Searches all user profiles under C:\\Users\\*\\Documents\\<CaseName>\\
  - New autopsy_find_cases tool: list all Autopsy cases on the system
  - All tools accept either db_path (full path) OR case_name (auto-resolve)

Improvements over v1:
  - Fixed zero-value loss bug in attribute extraction (0 != falsy)
  - Added missing TSK type mappings (INTERESTING_ITEM, ACCOUNT, ENCRYPTION_SUSPECTED, etc.)
  - Connection context manager prevents connection leaks on exceptions
  - Smart timestamp detection (seconds vs milliseconds)
  - Dedicated autopsy_get_keyword_hits tool with keyword filtering
  - Timeline event type filtering
  - MIME type filter in file search
  - Unified Pydantic model for autopsy_file_metadata
  - autopsy_triage: one-shot case summary for rapid triage
  - Richer pagination metadata block
"""
